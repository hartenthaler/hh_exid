# Screenshots

Place screenshots for the module documentation in this directory. Use concise,
stable filenames such as `admin-authority-catalogue.png` and
`admin-gedcom-contexts.png`. Screenshots should not contain real personal data;
use a test tree or anonymized values before committing them.
